# DESIGN_RULES.md

## Визуальная философия

## Тон текста интерфейса

## Главный экран: композиция

## Карточки `.duo`

## Закрытые / открытые карточки

## Историческая справка

Итоговый стандарт модуля: [`docs/HISTORY_CARD_STANDARD.md`](HISTORY_CARD_STANDARD.md) (v1.0).

### Обязательная проверка влезания

Перед `verified` для даты проверить текстовое состояние:

- ширина экрана **430px**;
- высота карточки **210px**;
- контент без обрезки и без внутреннего скролла.

### Запрещено при влезании контента

- уменьшать кегль ради «запихнуть» текст;
- `line-clamp` и аналогичная обрезка строк;
- внутренний скролл в карточке;
- скрытая обрезка (`overflow: hidden` как способ спрятать невлезающий текст).

Если не помещается — укоротить тексты, а не ломать вёрстку.

## Empty-state

## Изображения

Для History: у главного события желательно локальное непустое `image` в `assets/history/`. Если у главного события image нет, закрытая карточка может отображаться без фото (другое событие на его место не подставляется).

В редакционной тройке даты желательно **минимум одно** событие с рабочим `image` у главного. Если у главного события image нет — дата может считаться **редакционно незавершённой**: нужно подготовить image для главного или пересмотреть редакционные ранги, но UI не подменяет событие.

## Цвет и типографика

## Motion

## Что нельзя делать

## История и «Открытие дня»: визуальный паритет

## Выбор исторических событий

### Цель модуля

Главная задача — показать **самое интересное** событие этого дня, а не самое важное в академическом смысле.

### Выбор события для показа (UI)

`verified`-события даты сортируются по `interest → priority → year`.

На экране показывается **первое** после этой сортировки (`verified[0]`). Отсутствие `image` **не меняет** выбор.

Закрытая и открытая карточка показывают **одно и то же** событие. Если у главного нет image — закрытая карточка может рендериться без фото; другое событие не подставляется.

### Редакционная завершённость даты

Единый редакционный статус даты:

- **Редакционно завершена** — выполнены все редакционные критерии проекта; у главного (показываемого) события есть локальное `image`.
- **Редакционно незавершена** — хотя бы один критерий не выполнен (в т.ч. нет `image` у главного, слабый источник, текст, календарь и т.п.).

Исключение «завершена без image» / `image unavailable` **не используется**.

### Критерии отбора

1. Вызывает удивление или любопытство.
2. Понятно обычному человеку за 15–20 секунд.
3. Имеет красивое или выразительное изображение.
4. Достоверный факт с проверенными источниками.
5. Предпочтение — позитивным, научным, культурным, природным и космическим событиям.
6. На дату в данных хранить до **трёх** событий (редакционная подборка: главное + остальные отобранные).
7. Подборка должна быть максимально **интересной и разнообразной**.
8. Пользователю показывается **только одно** событие дня — первое после сортировки `interest → priority → year`. Отсутствие `image` не меняет выбор; закрытая и открытая карточка — одно событие. Остальные события редакционной подборки остаются в JSON и **не показываются** в текущем UI (Решение №009).

### Страны пользователя и region

- У события: `region: WORLD` или код страны (`RU`, `FI`, `KZ`, `US` и т.д.).
- При выборе учитывать страну пользователя.
- Если есть интересное национальное событие — оно получает преимущество при выборе главного.
- Если нет — выбирать лучшее мировое.
- При наличии сильного национального события оно **желательно** входит в подборку.
- **Регион не является ограничением:** события разных регионов можно сочетать; разнообразие тем важнее формального разведения по `region`.

### Приоритет тем (ориентир, не жёсткий порядок)

Наука и открытия · космос · культура · природа · значимые мировые события · национальная история (участник подборки при наличии сильного факта).

## Редакционная оценка событий

Каждое событие оценивается по 5 критериям (1–10):

1. 😲 Удивляет.
2. 🧠 Даёт новое знание.
3. 🖼️ Имеет сильное изображение.
4. 📖 Хочется открыть «Подробнее».
5. 😊 Делает утро немного интереснее.

Максимум — 50 баллов.

Дополнительное правило: если после прочтения возникает желание рассказать об этом другому человеку — событие соответствует концепции «Мой день».

## Редакционная оценка подборки

После выбора лучших событий оценивается не только каждое событие отдельно, но и вся подборка.

Критерии:
- разнообразие тем;
- отсутствие повторов;
- разные эпохи;
- сочетание мировых и национальных событий (если есть достойные);
- редакционная тройка должна ощущаться для редакции как разные интересные истории; пользователю при этом показывается только главное событие (Решение №009).

## Адаптив

## Проверка перед релизом модуля UI
